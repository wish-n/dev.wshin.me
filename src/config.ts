const config = {
  /**
   * 블로그 메타 정보
   */
  meta: {
    blogTitle: "wsh-n",
    blogDescription: "Hello, world!",
  },
  /**
   * 네비게이션 바에 노출할 링크 목록
   */
  navLinks: [
    { name: "Posts", url: "/" },
    { name: "Archive", url: "/archive" },
  ],
};

export default config;
