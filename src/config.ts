const config = {
  meta: {
    blogTitle: "wsh-n",
    blogDescription: "Hello, world!",
  },
  navLinks: [
    { name: "Posts", url: "/" },
    { name: "Archive", url: "/archive" },
  ],
};

export default config;
